import { Routes } from "@angular/router";
import { CustomMapComponent } from "./custom-map.component";

export const CustomMapRoutes: Routes  = [
    {
        path: '',
        component: CustomMapComponent,
    }
]