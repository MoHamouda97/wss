import { Routes } from "@angular/router";
import { LeafletComponent } from "./leaflet.component";

export const LeafletRoutes: Routes  = [
    {
        path: '',
        component: LeafletComponent,
    }
]