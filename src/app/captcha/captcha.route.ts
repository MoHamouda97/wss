import { Routes } from "@angular/router";
import { CaptchaComponent } from "./captcha.component";

export const CaptchRoutes: Routes  = [
    {
        path: '',
        component: CaptchaComponent,
    }
]